# CLAUDE.md — AI 회사 운영 규칙

## AI 회사 체계
이 프로젝트는 핫플리케이션 AI 회사 체계로 운영한다. 7개 부서가 `.claude/agents/` 에 서브에이전트로 있다:
`ceo`(대표실) · `marketing`(마케팅) · `sales`(영업) · `support`(고객지원) · `finance`(재무) · `build`(개발) · `ops`(운영)

- **공통 컨텍스트**: `AI회사/회사.md` — 모든 부서가 일 시작 전에 읽는다. 목표·규칙·인수인계 규약이 여기 있다.
- **인수인계**: 부서 결과물은 `AI회사/인수인계/YYYY-MM-DD-부서-주제.md` 에 헤더를 붙여 저장한다. 앞 팀 결과물은 **요약하지 않고 그대로** 다음 팀 입력으로 넘긴다.
- **커맨드**: `/plan-month`(월간 세팅) · `/standup`(데일리) · `/handoff`(인수인계 + 다음 팀 호출)

## 오케스트레이션 규칙 (메인 세션용)
1. 방향·우선순위가 필요한 요청은 `ceo` 에게 먼저 보낸다. 어느 부서인지 명확하면 바로 그 부서를 호출한다.
2. 부서 하나가 실제 결과물 하나를 끝내는 게 기본 단위다. 전 부서를 한꺼번에 돌리지 않는다.
3. 부서가 `⚠️ 사람 결정 필요` 를 올리면 자동으로 다음 팀을 호출하지 말고 종인에게 먼저 보여준다.
4. 부서 결과물의 완료 조건이 ⬜ 이면 다음 팀으로 넘기지 않는다. 이유를 보고한다.
5. 종인에게는 반말·결론부터·짧게 (`AI회사/회사.md` §5-7). 코드 주석·커밋 메시지는 한국어.

## ⚠️ 2026-08-25 이후 개발 시 필독
`saju-web/HANDOFF-2026-08-25.md` 를 **코드 만지기 전에** 읽는다. 실사용자 9명 검증에서 치명 버그
(엔진이 계산한 신강/신약이 AI에게 전달되지 않아 판정이 뒤집힘)를 포함해 다섯 가지를 고쳤고,
프롬프트 규칙이 왜 그렇게 짜여 있는지가 거기 있다. 모르고 손대면 되돌려놓기 쉽다.

특히 되돌리면 안 되는 것: 엔진 확정 판정 블록 / 사건 단정 금지 / 본문 한자 금지 /
금전·관계·타인건강 안전 규칙 / 프리미엄 캐싱 금지. 자세한 건 `.claude/agents/build.md` §[절대 건드리면 안 되는 것].

## 이 레포의 기존 규칙이 우선
이 레포 CLAUDE.md 의 기존 절대 규칙(테스트 선행, 유파 항목 옵션화, 금지어 검수 우회 금지, API 키 .env, 바틀 자산 미사용)은 AI 회사 규칙보다 우선한다.
