# 지금부터 종인님이 직접 해야 할 것 (2026-08-23 v2 — 순서 확정판)

> 개발(코드)은 계속 진행합니다. 아래는 제가 대신할 수 없는 것 — 계정 개설, 법적 신고, 대표 결정 — 만 모았어요.
> **v2 수정**: 사업자등록증이 아직 발급 전인 것으로 확인 → 순서 재편. 그리고 토스 **테스트 키는 사업자등록 없이 발급 가능**(개발자센터 이메일 가입 → 개발 연동 체험 상점 키)이라 토스는 막힌 항목이 아닙니다. 라이브 키(전자결제 신청)만 사업자등록증 필요.

## 실행 루트 (이 순서대로)

### A. 오늘 바로 가능 — 사업자등록 불필요
- [ ] **A1. 토스페이먼츠 개발자센터 가입** (developers.tosspayments.com, 이메일만) → 테스트 클라이언트 키 + 시크릿 키를 저한테 전달(또는 `.env.local`에 입력). 결제위젯(카카오페이 포함) 전체 흐름을 실키로 테스트할 수 있게 됩니다.
- [x] **A2. Supabase 프로젝트 생성** — 완료 (2026-08-23, 종인 승인). `mfhjzxcyjqrtsxvjmgmo` 조직에 `saju-app` 프로젝트 생성(ap-northeast-2, 무료 티어). URL·anon/publishable key는 `saju-web/.env.local`에 이미 반영됨.
  - ⚠️ **남은 것**: `SUPABASE_SERVICE_ROLE_KEY`는 보안상 프로그램으로 조회 불가 — [Supabase 대시보드](https://supabase.com/dashboard/project/byhiztmqvzaigmqddabs/settings/api-keys) → Settings → API Keys에서 `service_role` 키를 직접 복사해서 `.env.local`에 붙여넣어야 함.
  - ⚠️ **부수 조치**: 무료 프로젝트 계정 한도(2개)에 걸려서, 종인 승인 하에 기존 `insurance-lead`(보험AI) 프로젝트를 일시정지함. 재개하려면 Supabase 대시보드에서 Restore. 보험AI 재우선순위 논의 시 참고.
  - ⬜ **다음 단계(미구현)**: `readings` 테이블 마이그레이션(스펙: `PHASE3-SPEC.md` "백엔드" 항목) 아직 안 만듦 — service_role 키 받으면 개발팀이 이어서 적용.
- [ ] **A3. 브랜드명 확정** — 실질적 최상위 병목. 도메인·SNS 계정(영업킷 실행)·통판신고 표기·이메일 문구가 전부 여기 걸려 있어요. 후보: 정밀사주 · 명식 · 원국 · 일간 · 초분 · 만세.
- [ ] **A4. 사업자등록 상태 확인** — 아직 신청 전이면 홈택스에서 바로 신청(무료, 보통 즉시~3영업일). 사업자 상호는 서비스 브랜드명과 달라도 되므로 A3을 기다릴 필요 없이 지금 진행 가능해요.

### B. 브랜드명 확정 직후
- [ ] **B1. 도메인 구매** → 저한테 알려주시면 링크·이메일 주소 반영.
- [ ] **B2. Resend 계정 + 발신 도메인 인증** (B1 이후에만 가능 — DNS 레코드 추가 필요).
- [ ] **B3. SNS 계정 개설** (X 또는 스레드 — 채널 결정과 함께) → 영업킷·마케팅 오가닉 루프 실행 시작.

### C. 사업자등록증 나온 뒤
- [ ] **C1. 통신판매업 신고** (정부24, 사업자번호 필요). 선지급식 결제라 구매안전서비스 이용확인증이 필요할 수 있는데, 토스 전자결제 신청 과정에서 받을 수 있어요. 신고번호는 결제 화면 하단 표기.
- [ ] **C2. 토스페이먼츠 전자결제 신청 → 라이브 키 발급** (실결제 오픈 조건).

### D. 출시 직전
- [ ] **D1. 호스팅 결정 + 계정** — 프리미엄 생성 15~20분이 Vercel 함수 시간제한 초과 → Railway/Fly.io 권장, 최저 티어로 충분. 개발·테스트는 그동안 로컬로 계속 가능.
- [ ] **D2. 개인정보처리방침 최종 검토** — 초안은 제가 작성(생년월일시·고민 텍스트 취급 + "고민 텍스트 익명 통계 활용" 조항 포함), 최종 확인만 해주세요.

### E. 상시 운영
- [ ] **E1. Anthropic API 크레딧 관리** — 자동 충전 or 잔고 알림 설정 권장 (이미 두 번 생성 중단 겪음).
## 후순위 — 대표 판단 항목 (급하지 않음, 나올 때마다 알려주시면 됩니다)

- [ ] 희소도 배지 문구 수위 (예: "水가 0개인 명식은 전체의 N%" 같은 계산된 수치를 얼마나 세게 노출할지)
- [ ] 지시형 솔루션 문구 수위 ("~하세요" 같은 단정적 어투를 얼마나 쓸지)
- [ ] 후속 질문 채팅 기능 추가 여부
- [ ] 궁합 3종 세트 상품화 여부
- [ ] 캐릭터 페르소나(화자) 도입 여부 — 용용사주의 "현운" 같은 것

## 병행 트랙 (급하지 않음)

- [ ] **이태정님 피드백** — 본인 리딩 받아보고 피드백 주면 반영 여부 결정 (본인이 명시적으로 미룬 항목, 급하지 않음)
- [ ] **박형준님 합류** — 영업팀 핸드오프 문서 + 영업킷(`인수인계/2026-08-23-영업팀-고민중심-오가닉-영업킷.md`) 전달 완료. 형준님이 코워크에서 `코워크` 폴더 연결하고 시작하면 됩니다.

---

**참고**: 이 목록의 원본은 `회사.md`(§3 사람 결정 필요)와 `saju-web/README.md`(법적 요건 체크리스트)에도 나눠서 기록돼 있습니다. 여기서 체크된 걸 저한테 알려주시면 그쪽 문서도 같이 업데이트할게요.
